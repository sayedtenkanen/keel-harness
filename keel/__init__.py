"""Keel — a context-managed, provider-agnostic agent harness.

The context window is a cache over an addressable store. Layout and budget are
policy, not accident. Every quality claim is a check that runs after the fact.
"""

__version__ = "0.0.1"
